package com.fish_dan_.data_energistics.api.registry.adaptive;

/** Direction of a connector route relative to its bound adaptive provider. */
public enum AdaptiveProviderConnectorDirection {
    INPUT,
    RETURN
}
