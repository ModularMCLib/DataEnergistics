package com.fish_dan_.data_energistics.item.connector;

/** Identifies the kind of object stored in a distribution connector binding. */
public enum ConnectorHostType {
    TOWER,
    ADAPTIVE_PROVIDER,
    EXTREME_INTERFACE
}
