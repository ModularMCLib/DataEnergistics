# 数位化封包供应器：阶段交付记录

本分支是完整封包计划的阶段实现，尚未达到发布条件。相对原计划的实现及验证覆盖度估算约为 **55%**，不是按代码行数计算。

## 已接入的公共路径

- 通过项目插件生命周期注册适配器，按已安装 Mod 隔离可选 API。
- 复用加工样板的配方类型和配方 ID；兼容配方不受普通类型记录开关关闭的影响。
- 独立供应器有 36 个样板槽和两行共 18 个回收槽。回收槽容量、NBT 和菜单资源数量均使用 `long`。
- 独立供应器只发现相邻机器；自适应供应器可识别同维度连接器目标，没有扩展 ConnectorLink 的存档格式。
- 自适应封包分派先尝试远程目标，再尝试相邻兼容机器，不进入普通库存投料路径。
- 每个机器类别拥有独立轮询游标和顺序优先策略状态；只有成功接受完整输入后推进游标。
- 接单时固定机器和已选部件；原子预留共享基座、注入器等物理位置，阻止重叠机器竞争。
- 任务保存配方引用、已投料进度、尚持有的输入和已回收输出；拥有数量按 `BigInteger` 记账，物理机器操作使用有界数量。
- 掉落物通过真实机器调用范围和任务 UUID 追踪；不同任务的掉落物禁止合并。
- 可重复诊断由独立配置 `developer.packagedCraftingLogging` 控制。
- 拆除时将封包任务、回收库存和完整 `BigInteger` 数量写入维度存档恢复凭证；凭证绑定原维度、位置和供应器类型，只能成功兑换一次。
- 恢复凭证不会复制正在执行的任务资产；恢复前不会释放机器占用，恢复后继续沿用原任务 UUID 和物料账本。

## 机器实现范围

| Mod | 已写入的路线 | 未完成或验证限制 |
| --- | --- | --- |
| Mystical Agriculture | 注魔祭坛、觉醒祭坛 | 已有真实批次、返还物、结构与进度恢复用例；尚需完整宿主生命周期验证 |
| Ars Nouveau | 附魔装置、灌注室 | 相同输入输出的灌注完成识别、混合替代材料批次仍有限制 |
| Occultism | 灵火、确定物品产出的部分仪式 | 非确定召唤、升级及额外献祭/玩家交互类仪式尚未完整覆盖 |
| Draconic Evolution | 真实聚合核心与注入器 | 真实供能、材料消耗、产物回收及配方冲突接单拒绝测试已通过；特殊多数量 fusion ingredient 未支持 |
| Botania | 花药台、魔力池、精灵传送门、泰拉凝聚板、符文祭坛 | 传送门要求预先开启；水桶样板要求空盆；特殊配方及编码需进一步整合验证 |
| Extended Crafting | 末影合成器、通量合成器、四级工作台、组合合成核心 | 组合核心已覆盖原生能耗、产物和桶返还物；组合配方的更多特殊余物仍需继续验证 |
| Actually Additions | 充能台 | 原子再构机未完成，充能台尚未实际运行验证 |
| Malum | 精魂灌注、精魂聚焦代码 | 尚未完成真实机器回归验证 |
| Avaritia | Sculk、Nether、End、Extreme 四级工作台 | Extreme Smithing 使用临时菜单库存，未接入持久封包执行 |
| Mekanism More Machines | 尚未接入 | 六类大型机器待实现 |

## 后续待完成的工作

1. 完成余下适配，验证所有 Mod 缺失时的启动路径。
2. 扩展拆除恢复到所有供应器类型、失败任务和跨重启占用释放；当前普通 AE2 大数量掉落路径仍存在数量截断风险，不能作为完整恢复实现。
3. 验证网络满、卸载、重启、删除链接和更换内装供应器；尤其检查持久化任务与维度占用记录的一致性。
4. 完成三位一体超大订单、绑定输入、不可合批机器持续执行的端到端验证。存在 BigInteger 记账并不代表全部大数量入口已验证。
5. 补齐实际 JEI/EMI 分类与服务端配方 ID 映射、辅助耗材和返还物编码验证。
6. 完成配方重载缓存失效、目标索引及结构变化的性能与正确性验证。

## 已完成的验证记录

- 早期完整 GameTestServer 两轮分别为 301/301、302/302 必需测试通过。
- 302 用例轮包含独立供应器实际菜单的 36 个样板槽、18 个回收槽，以及最后一槽 Long.MAX_VALUE 数量显示。
- 2026-09-19 聚合修复后，`compileJava compileTestJava jar runGameTestServer` 完整执行通过，310/310 必需测试通过。
- 聚合及 Botania 定向 GameTest 为 7/7 通过。聚合接单使用完整批次数量的只读快照检查原生配方选择；被其它配方覆盖的 ID 在投料前拒绝。
- 聚合测试覆盖 Wyvern 原生充能和合成的完整周期，验证输入消耗、注入器能量耗尽，以及回收账本中实际产物的类型和数量。
- 三个聚合改动文件的 IDEA 错误检查、限定文件的 Spotless 检查及 UTF-8 无 BOM 检查通过。
- 恢复生命周期与 Extended Crafting 组合核心定向 GameTest 共 11/11 通过，覆盖容量卡初始化、实际拆除、凭证重载、防重复领取、`BigInteger` 资源、原生组合能耗和桶返还物。
- 上述历史通过结果不能代表后续全部变更已通过；最终结果以本 PR 的验证记录为准。

## 依赖约定

新增依赖采用远程坐标，官方 Maven 优先，未找到可用官方发布时使用 CurseForge，Modrinth 最后考虑；不使用本地 jar。测试编译依赖集中声明，别名不带 `-packaged` 后缀。Gradle 缓存由环境变量 `GRADLE_USER_HOME` 提供。
